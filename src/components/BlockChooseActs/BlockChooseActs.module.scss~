.container {
    @include flex-left-column;

    max-width: 400px;
    border-radius: 10px;
    box-shadow: $shadow;
    padding: 10px;
    gap: 5px;
    background-color: $background-color-container;
}

.list {
    margin-left: 18px;
    height: calc(100vh - 500px);
    overflow-x: auto;
}
